import { createLibrary, ILibrary } from "../src";

describe('library', () => {
    let library: ILibrary;
    
    beforeEach(() => {
        library = createLibrary();
    });
    
    it('should call multer disckStorante');
    
    it('should call multer');
    
    describe("deleteFile", () => {
        it('should call fs.unlink');
    })
});
